---
import type { Props } from '@astrojs/starlight/props';
import type { Icon } from '@astrojs/starlight/components';


// Define the props for the ProfileLinks component
export interface ProfileLinksProps {
  Icon: IconProps[]; // Array of IconProps to represent each icon
}

// Define the IconProps interface for the Icon component
export interface IconProps {
  name: string;        // The name of the icon (e.g., "star")
  color?: string;      // Optional color (e.g., "goldenrod")
  size?: string;       // Optional size (e.g., "2rem")
  link: string;        // The URL for the anchor element
}
---
```astro


<style>
    .profileLinks {
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 10px 20px;
        border-radius: 10px;
        box-shadow: 0 2px 15px var(--box-shadow);
        margin-right: 15px;
    }
    .profileLinks a {
        display: inline-block;
        font-family: 'Helvetica', sans-serif;
        font-size: 0.9rem;
        color: var(--text-light);
        padding: 10px 15px;
        text-decoration: none;
        border-radius: 5px;
        transition: background-color 0.3s, color 0.3s;
    }
</style>

